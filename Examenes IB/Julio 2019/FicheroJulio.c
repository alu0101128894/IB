/* Escribir un programa que dado un fichero "datos.txt" de numeros entero (un numero por linea), abra, 
lea el fichero e imprima en un segundo fichero todos aquellos numeros que sean multiplos de 5 y pares a la vez.
Para ello hacemos el uso de un segundo fichero. "multiplo.txt" */
